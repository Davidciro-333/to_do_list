import 'package:flutter/material.dart';
import 'package:to_do_list/app/app.dart';

/// The main entry point of the application.
///
/// This function is called when the application starts and initializes
/// the Flutter framework by calling [runApp] with the root widget of the
/// application.
void main() {
  runApp(const MyApp());
}